#include <stdio.h>

int main(void){
	int a[5] = {1,2,4,6,8};
	int (*p)[5] = &a;      //因为运算符的优先性，此处要加()

	//*p == a            //因为运算符的优先性，此处不需要加()
	//(*p)[2]  == a[2]   //因为运算符的优先性，此处要加()
	printf("%d\n",(*p)[2]);

	return 0;
}